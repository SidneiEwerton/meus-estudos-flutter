import 'package:flutter/material.dart';

abstract class Appicons {
  static  const deletar = Icons.delete;
  static const email = Icons.email;
  static const senha = Icons.visibility;
  static const obscure = Icons.visibility_off;
}